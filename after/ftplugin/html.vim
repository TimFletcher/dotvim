set tabstop=2                     " Global tab width - Affects tabs already in a file.
set shiftwidth=2                  " Width of > and < commands in visual mode
set softtabstop=2                 " Backspace in insert mode removes X spaces
set expandtab                     " Use spaces instead of tabs